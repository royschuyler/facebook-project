h1 Facebook-project
h2 Search For Friends
input(placeholder="Search", ng-model="query")
h2 Profiles
a.btn.btn-primary(href="#/people/new") New Profile
.gallery
  .person(ng-repeat="(id, person) in main.people | objToArr | orderBy:'name' | filter:query")
    a(href="#/people/{{person._id}}")
      img(ng-src="{{person.photo}}")

div(ng-include="'assets/people/modal.html'" onload="main.onModalLoad()")
